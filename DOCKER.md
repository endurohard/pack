# 🐳 Развертывание Invoice Generator в Docker

## Описание

Этот сервис позволяет генерировать счета в PDF, отправлять их через WhatsApp и автоматически рассылать по расписанию.

**Возможности:**
- Генерация PDF счетов
- Отправка через WhatsApp
- Автоматическая рассылка по расписанию
- Загрузка на Яндекс.Диск (опционально)
- Управление базой счетов
- Адаптивный веб-интерфейс

## Требования

- Docker 20.10+
- Docker Compose 2.0+
- Минимум 2GB RAM
- Минимум 5GB свободного места на диске

## Быстрый старт

### Вариант 1: Docker Compose (рекомендуется)

```bash
# 1. Клонировать или скопировать проект на сервер
cd /path/to/invoice-generator

# 2. Создать необходимые директории
mkdir -p data invoices uploads wwebjs_auth wwebjs_cache

# 3. Собрать и запустить
docker-compose up -d

# 4. Просмотреть логи
docker-compose logs -f

# 5. Открыть в браузере
# Локально: http://localhost:3000
# На сервере: http://your-server-ip:3000
```

### Вариант 2: Docker напрямую

```bash
# Собрать образ
docker build -t invoice-generator .

# Запустить контейнер
docker run -d \
  -p 3000:3000 \
  -v $(pwd)/data:/app/data \
  -v $(pwd)/invoices:/app/invoices \
  -v $(pwd)/uploads:/app/uploads \
  -v $(pwd)/wwebjs_auth:/app/.wwebjs_auth \
  -v $(pwd)/wwebjs_cache:/app/.wwebjs_cache \
  --security-opt seccomp=unconfined \
  --shm-size=2gb \
  --name invoice-generator \
  invoice-generator
```

## Первичная настройка WhatsApp

После запуска контейнера необходимо авторизовать WhatsApp:

```bash
# 1. Смотрим логи контейнера
docker-compose logs -f

# 2. При первом запуске появится QR-код для авторизации WhatsApp
# 3. Откройте WhatsApp на телефоне: Настройки → Связанные устройства → Привязать устройство
# 4. Отсканируйте QR-код из логов

# После успешной авторизации увидите:
# ✅ WhatsApp Web готов к использованию!
```

**Важно:** Авторизация сохраняется в volume `wwebjs_auth`, поэтому после перезапуска контейнера повторная авторизация не требуется.

## Настройка Яндекс.Диска (опционально)

### Способ 1: Через .env файл

```bash
# 1. Создайте .env файл
cp .env.example .env

# 2. Получите OAuth токен на https://yandex.ru/dev/disk/poligon/

# 3. Добавьте токен в .env
nano .env
```

Содержимое `.env`:
```env
YANDEX_DISK_TOKEN=ваш_токен_здесь
YANDEX_DISK_FOLDER=/Счета
```

```bash
# 4. Раскомментируйте монтирование .env в docker-compose.yml
nano docker-compose.yml

# Найдите и раскомментируйте:
# - ./.env:/app/.env:ro

# 5. Перезапустите контейнер
docker-compose restart
```

## Управление контейнером

```bash
# Посмотреть логи
docker-compose logs -f

# Остановить
docker-compose down

# Перезапустить
docker-compose restart

# Пересобрать и запустить
docker-compose up -d --build
```

## Структура директорий и данные

Все данные сохраняются в примонтированных volumes:

```
├── data/                  # База данных счетов (invoices.json)
├── invoices/             # Сгенерированные PDF счета
├── uploads/              # Загруженные файлы
├── wwebjs_auth/          # Данные авторизации WhatsApp
└── wwebjs_cache/         # Кеш WhatsApp
```

```bash
# Посмотреть созданные счета
ls -lh invoices/

# Посмотреть базу данных
cat data/invoices.json | jq .

# Проверить размер директорий
du -sh data invoices uploads wwebjs_auth wwebjs_cache
```

## Автоматическая рассылка

Планировщик автоматически запускается вместе с приложением и работает по следующей схеме:

- **Первая проверка**: через 1 минуту после запуска
- **Последующие проверки**: каждые 10 минут
- **Задержка между отправками**: 10 минут

Для включения авторассылки для счета:
1. Откройте веб-интерфейс
2. Создайте счет с указанием телефона клиента
3. Включите галочку "Повторяющийся"
4. После создания включите "Авторассылку" в таблице счетов

Логи автоматической рассылки:
```bash
docker-compose logs -f | grep AutoSend
```

## Порты

По умолчанию сервис доступен на порту **3000**.

Для изменения порта отредактируйте `docker-compose.yml`:

```yaml
ports:
  - "8080:3000"  # Теперь доступен на http://localhost:8080
```

## Проблемы и решения

### Порт уже занят

```bash
# Найти процесс на порту 3000
lsof -i :3000

# Или измените порт в docker-compose.yml
ports:
  - "8080:3000"  # Используем порт 8080 вместо 3000
```

### WhatsApp не подключается

```bash
# 1. Проверьте логи
docker-compose logs -f | grep WhatsApp

# 2. Удалите данные авторизации и попробуйте снова
rm -rf wwebjs_auth wwebjs_cache
docker-compose restart

# 3. Отсканируйте новый QR-код
docker-compose logs -f
```

### Авторассылка не работает

```bash
# 1. Проверьте логи планировщика
docker-compose logs -f | grep AutoSend

# 2. Убедитесь что у счета:
# - Включена авторассылка (autoSendEnabled: true)
# - Указана дата отправки (nextSendDate)
# - Указан телефон клиента (clientPhone)

# 3. Проверьте базу данных
cat data/invoices.json | jq '.[] | select(.autoSendEnabled == true)'
```

### Chrome/Puppeteer падает с ошибкой

```bash
# Увеличьте shared memory
# В docker-compose.yml:
shm_size: '4gb'  # Было 2gb

# Или добавьте в Dockerfile:
ENV PUPPETEER_ARGS="--no-sandbox --disable-setuid-sandbox"
```

### Файлы не сохраняются

```bash
# Проверьте права доступа
ls -la data invoices uploads

# Исправьте права если нужно
sudo chown -R 1000:1000 data invoices uploads wwebjs_auth wwebjs_cache

# Проверьте volume
docker-compose config
```

### Яндекс.Диск не работает

```bash
# 1. Проверьте, что токен добавлен
cat .env | grep YANDEX_DISK_TOKEN

# 2. Проверьте монтирование в docker-compose.yml
# Раскомментируйте:
# - ./.env:/app/.env:ro

# 3. Проверьте логи
docker-compose logs | grep Yandex
```

## Production deployment

### С использованием nginx

Создайте `nginx.conf`:

```nginx
server {
    listen 80;
    server_name example.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

### С SSL (HTTPS)

Используйте Let's Encrypt + Certbot или Cloudflare.

## Характеристики

- **Образ**: Node.js 20 Bullseye (~1GB - включает Chromium для Puppeteer)
- **Порт**: 3000 (настраивается)
- **Память**: ~500MB-1GB (с учетом Chrome)
- **CPU**: Рекомендуется 2+ ядра
- **Диск**: ~2-5GB (зависит от количества счетов)

## Healthcheck

Контейнер автоматически проверяет свое состояние каждые 30 секунд.

Проверить вручную:

```bash
docker-compose ps
```

## Обновление

```bash
# Получить последние изменения
git pull

# Пересобрать и запустить
docker-compose up -d --build
```

## Удаление

```bash
# Остановить и удалить контейнер
docker-compose down

# Удалить образ
docker rmi invoice-generator

# Удалить все (включая volumes)
docker-compose down -v
```

## Мониторинг

```bash
# Статистика ресурсов
docker stats invoice-generator

# Логи в реальном времени
docker-compose logs -f --tail=100
```

---

**Готово!** Откройте http://localhost:3000 и создавайте счета! 🚀
