# 🚀 Простая настройка (почти как логин/пароль)

К сожалению, Яндекс.Диск не поддерживает прямую аутентификацию по логину/паролю (это требование безопасности).

Но я сделал максимально простую настройку - **нужно один раз получить токен, и всё!**

## Шаг 1: Первая настройка (один раз)

Запустите команду:

```bash
npm run setup
```

Она попросит вас:

1. Перейти по ссылке https://yandex.ru/dev/disk/poligon/
2. Нажать "Получить OAuth-токен"
3. Разрешить доступ
4. Вставить токен

**Это нужно сделать только один раз!** Токен сохранится и больше не потребуется.

## Шаг 2: Готово! Используйте

После настройки просто запускайте:

```bash
npm run example:simple
```

И счета **автоматически** будут:
- ✅ Создаваться локально
- ✅ Загружаться на Яндекс.Диск в папку `/Счета`

## Пример кода - супер простой!

```javascript
import SimpleInvoiceService from './src/simpleInvoiceService.js';

// Создаем сервис - он сам загрузит все настройки!
const service = new SimpleInvoiceService();

// Создаем счет
const invoice = {
  invoiceNumber: 123,
  items: [
    service.createItem('Услуга 1', 'шт', 2, 1000.00),
    service.createItem('Услуга 2', 'час', 5, 500.00)
  ],
  discount: service.createPercentDiscount(10),
  payment: {
    cardNumber: '1111-2222-3333-4444',
    sbpPhone: '9001112233',
    sbpBank: 'Банк'
  }
};

// ВСЁ! Он сам создаст PDF и загрузит на Яндекс.Диск
await service.createInvoice(invoice);
```

## Почему нельзя по логину/паролю?

Современные облачные сервисы (Google Drive, Dropbox, Яндекс.Диск) НЕ разрешают прямой доступ по логину/паролю из приложений. Это сделано для:

1. **Безопасности** - токен можно отозвать, пароль нельзя
2. **Контроля** - токен дает только нужные права, не полный доступ к аккаунту
3. **Стандарта OAuth 2.0** - это индустриальный стандарт безопасности

## Преимущества токена

✅ Настраивается один раз
✅ Не нужно вводить пароль
✅ Можно отозвать в любой момент
✅ Не дает доступ ко всему аккаунту
✅ Работает автоматически после настройки

## Альтернатива: Без Яндекс.Диска

Если не хотите настраивать Яндекс.Диск - сервис будет работать локально:

```bash
npm test
```

Счета будут сохраняться в папку `output/`.

---

**Итого:** Один раз потратить 1 минуту на получение токена = потом всё работает автоматически! 🎉
