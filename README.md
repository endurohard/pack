# Invoice Generator

Сервис для генерации счетов в формате PDF с поддержкой русского языка, QR-кодов для оплаты и автоматической загрузкой на Яндекс.Диск.

## Возможности

- 🌐 **Веб-интерфейс** - удобный сайт для создания счетов
- 🐳 **Docker** - запуск одной командой
- 📄 Генерация PDF счетов с таблицей товаров/услуг
- 💰 Поддержка процентных и фиксированных скидок
- 🧮 Автоматический расчет сумм
- 📱 QR-коды для оплаты
- ☁️ **Интеграция с Яндекс.Диском** - автоматическая загрузка счетов
- 🔗 Получение публичных ссылок на счета
- 🇷🇺 Поддержка русского языка

## 🚀 Быстрый старт

### Вариант 1: Docker (рекомендуется)

```bash
# Запустить сервис
docker-compose up -d

# Открыть в браузере
open http://localhost:3000
```

Готово! Создавайте счета через веб-интерфейс.

Подробнее: [DOCKER.md](DOCKER.md)

### Вариант 2: Локально

```bash
# Установка
npm install

# Запуск веб-сервера
npm start

# Открыть в браузере
open http://localhost:3000
```

### Вариант 3: Программный API

```bash
npm install
```

## ⚡ Быстрая настройка Яндекс.Диска (рекомендуется)

**Автоматическая настройка за 1 минуту:**

```bash
npm run setup
```

Скрипт сам проведет вас через процесс получения токена и настройки.

**Готово!** Теперь счета будут автоматически загружаться на Яндекс.Диск.

<details>
<summary>📖 Ручная настройка (альтернатива)</summary>

1. Скопируйте файл `.env.example` в `.env`:
```bash
cp .env.example .env
```

2. Получите OAuth токен:
   - Перейдите на https://yandex.ru/dev/disk/poligon/
   - Нажмите "Получить OAuth-токен"
   - Разрешите доступ к Яндекс.Диску
   - Скопируйте полученный токен

3. Добавьте токен в файл `.env`:
```
YANDEX_DISK_TOKEN=ваш_токен_здесь
YANDEX_DISK_FOLDER=/Счета
```

</details>

> **Примечание:** Настройка Яндекс.Диска полностью опциональна. Без неё сервис будет сохранять счета только локально.

## Установка шрифтов (опционально)

Для корректного отображения русского текста рекомендуется установить шрифты DejaVu Sans:

### macOS
```bash
# Скачиваем шрифты
curl -L https://github.com/dejavu-fonts/dejavu-fonts/releases/download/version_2_37/dejavu-fonts-ttf-2.37.tar.bz2 -o dejavu-fonts.tar.bz2
tar -xjf dejavu-fonts.tar.bz2
cp dejavu-fonts-ttf-2.37/ttf/DejaVuSans.ttf fonts/
cp dejavu-fonts-ttf-2.37/ttf/DejaVuSans-Bold.ttf fonts/
rm -rf dejavu-fonts-ttf-2.37 dejavu-fonts.tar.bz2
```

### Или вручную
1. Скачайте шрифты DejaVu Sans с https://dejavu-fonts.github.io/
2. Скопируйте файлы `DejaVuSans.ttf` и `DejaVuSans-Bold.ttf` в папку `fonts/`

**Примечание:** Если шрифты не установлены, система будет использовать встроенные шрифты, но русский текст может отображаться некорректно.

## Использование

### 🚀 Супер-простой способ (рекомендуется)

```javascript
import SimpleInvoiceService from './src/simpleInvoiceService.js';

// Создаем сервис - он автоматически загрузит настройки из .env
const service = new SimpleInvoiceService();

// Создаем счет
const invoice = {
  invoiceNumber: 1001,
  items: [
    service.createItem('Консультация', 'час', 3, 2000.00),
    service.createItem('Разработка', 'час', 10, 3500.00)
  ],
  discount: service.createPercentDiscount(10, 'Скидка постоянного клиента'),
  payment: {
    cardNumber: '5280-4137-5406-3845',
    sbpPhone: '9633707007',
    sbpBank: 'ТИНЬКОФФ'
  }
};

// ВСЁ! Счет создастся и автоматически загрузится на Яндекс.Диск
await service.createInvoice(invoice);

// Результат:
// ✅ PDF создан локально в output/
// ✅ Автоматически загружен на Яндекс.Диск в /Счета/
// ✅ Можно получить публичную ссылку
```

Запустить пример:
```bash
npm run example:simple
```

---

<details>
<summary>📚 Продвинутые примеры</summary>

### Базовый пример (без автозагрузки)

```javascript
import InvoiceGenerator from './src/invoiceGenerator.js';

const generator = new InvoiceGenerator();

const invoiceData = {
  invoiceNumber: 672,
  items: [
    generator.createItem('Аренда АТС\\Обслуживание', 'мес', 1, 5700.00)
  ],
  payment: {
    cardNumber: '5280-4137-5406-3845',
    sbpPhone: '9633707007',
    sbpBank: 'ТИНЬКОФФ'
  }
};

await generator.generateInvoice(invoiceData, './output/invoice_672.pdf');
```

### Пример со скидкой

```javascript
import InvoiceGenerator from './src/invoiceGenerator.js';

const generator = new InvoiceGenerator();

// Счет с процентной скидкой
const invoiceData = {
  invoiceNumber: 673,
  items: [
    generator.createItem('Аренда АТС\\Обслуживание', 'мес', 1, 5700.00),
    generator.createItem('IP-телефония', 'мес', 1, 2500.00),
    generator.createItem('Настройка оборудования', 'шт', 1, 3000.00)
  ],
  discount: generator.createPercentDiscount(10, 'Скидка постоянного клиента'),
  payment: {
    cardNumber: '5280-4137-5406-3845',
    sbpPhone: '9633707007',
    sbpBank: 'ТИНЬКОФФ'
  }
};

await generator.generateInvoice(invoiceData, './output/invoice_673.pdf');

// Счет с фиксированной скидкой
const invoiceData2 = {
  invoiceNumber: 674,
  items: [
    generator.createItem('Разработка сайта', 'шт', 1, 50000.00),
    generator.createItem('Техническая поддержка', 'мес', 3, 5000.00)
  ],
  discount: generator.createFixedDiscount(5000, 'Специальное предложение'),
  payment: {
    cardNumber: '5280-4137-5406-3845',
    sbpPhone: '9633707007',
    sbpBank: 'ТИНЬКОФФ'
  }
};

await generator.generateInvoice(invoiceData2, './output/invoice_674.pdf');
```

### Пример с загрузкой на Яндекс.Диск

```javascript
import InvoiceGenerator from './src/invoiceGenerator.js';

const generator = new InvoiceGenerator();

const invoiceData = {
  invoiceNumber: 675,
  items: [
    generator.createItem('Аренда АТС', 'мес', 1, 5700.00)
  ],
  payment: {
    cardNumber: '5280-4137-5406-3845',
    sbpPhone: '9633707007',
    sbpBank: 'ТИНЬКОФФ'
  }
};

// Генерируем и загружаем на Яндекс.Диск
const result = await generator.generateAndUploadToYandexDisk(
  invoiceData,
  './output/invoice_675.pdf',
  process.env.YANDEX_DISK_TOKEN,
  '/Счета/invoice_675.pdf',
  {
    createFolder: true,   // Создать папку, если не существует
    publish: true,        // Получить публичную ссылку
    deleteLocal: false    // Не удалять локальный файл
  }
);

console.log(`Загружено: ${result.remotePath}`);
console.log(`Публичная ссылка: ${result.publicUrl}`);
```

### Пример прямой работы с Яндекс.Диском

```javascript
import YandexDiskService from './src/yandexDiskService.js';

const yandexDisk = new YandexDiskService(process.env.YANDEX_DISK_TOKEN);

// Получить информацию о диске
const info = await yandexDisk.getDiskInfo();
console.log(`Использовано: ${info.used_space} из ${info.total_space} байт`);

// Создать папку
await yandexDisk.createFolder('/Счета');

// Загрузить файл
await yandexDisk.uploadFile('./output/invoice.pdf', '/Счета/invoice.pdf');

// Получить публичную ссылку
const publicUrl = await yandexDisk.publishFile('/Счета/invoice.pdf');

// Список файлов
const files = await yandexDisk.listFiles('/Счета');

// Удалить файл
await yandexDisk.deleteFile('/Счета/old_invoice.pdf');
```

</details>

---

### Запуск примеров

```bash
# Настройка Яндекс.Диска (один раз)
npm run setup

# Простой способ - автоматическая загрузка на Яндекс.Диск
npm run example:simple

# Примеры со скидками и несколькими позициями
npm run example:discount

# Продвинутые примеры с Яндекс.Диском
npm run example:yandex

# Базовый пример (без Яндекс.Диска)
npm test
```

Счета будут созданы в папке `output/` и при наличии токена также загружены на Яндекс.Диск.

## Структура данных

### InvoiceData
- `invoiceNumber` (number) - Номер счета
- `items` (InvoiceItem[]) - Массив товаров/услуг
- `discount` (Discount, опционально) - Скидка на весь счет
- `payment` (PaymentInfo) - Платежная информация

### InvoiceItem
- `name` (string) - Наименование товара/услуги
- `unit` (string) - Единица измерения (шт, мес, и т.д.)
- `quantity` (number) - Количество
- `price` (number) - Цена за единицу
- `amount` (number) - Сумма (price × quantity)

### Discount
- `value` (number) - Размер скидки (процент или фиксированная сумма)
- `type` (string) - Тип скидки: `'percent'` (процент) или `'fixed'` (фиксированная сумма)
- `description` (string, опционально) - Описание скидки

### PaymentInfo
- `cardNumber` (string) - Номер карты для оплаты
- `sbpPhone` (string) - Телефон для СБП
- `sbpBank` (string) - Название банка для СБП

## Структура проекта

```
invoice-generator/
├── src/
│   ├── simpleInvoiceService.js    # 🚀 Упрощенный сервис (рекомендуется)
│   ├── invoiceGenerator.js        # Основной класс генератора
│   ├── yandexDiskService.js       # Сервис работы с Яндекс.Диском
│   ├── setupYandexDisk.js         # Скрипт настройки
│   ├── types.js                   # Определения типов данных
│   ├── index.js                   # Точка входа модуля
│   ├── example-simple.js          # Простой пример (рекомендуется)
│   ├── example.js                 # Базовый пример
│   ├── example-with-discount.js   # Примеры со скидками
│   └── example-yandex-disk.js     # Продвинутые примеры с Яндекс.Диском
├── fonts/                         # Русские шрифты
├── output/                        # Выходные PDF файлы
├── .env.example                   # Пример конфигурации
├── .gitignore
├── package.json
├── README.md
├── EASY_SETUP.md                  # Упрощенная инструкция
└── QUICKSTART.md                  # Быстрый старт
```

## API

### SimpleInvoiceService (рекомендуется)

Упрощенный сервис, который автоматически управляет загрузкой на Яндекс.Диск.

#### `constructor(config)`

Создает экземпляр упрощенного сервиса.

**Параметры (все опциональны):**
- `config.yandexToken` (string) - OAuth токен (по умолчанию из .env)
- `config.yandexFolder` (string) - Папка на диске (по умолчанию '/Счета')
- `config.autoUpload` (boolean) - Автоматическая загрузка (по умолчанию true)
- `config.publishLinks` (boolean) - Получать публичные ссылки (по умолчанию false)
- `config.keepLocalCopy` (boolean) - Сохранять локальную копию (по умолчанию true)

**Пример:**
```javascript
// Автоматически загружает настройки из .env
const service = new SimpleInvoiceService();

// Или с кастомной конфигурацией
const service = new SimpleInvoiceService({
  yandexToken: 'мой_токен',
  yandexFolder: '/Мои_Счета',
  publishLinks: true
});
```

#### `createInvoice(invoiceData, filename)`

Создает счет и автоматически загружает его на Яндекс.Диск (если настроен).

**Параметры:**
- `invoiceData` (InvoiceData) - Данные счета
- `filename` (string, опционально) - Имя файла (генерируется автоматически)

**Возвращает:** Promise<Object> - Результат с путями и ссылками

**Пример:**
```javascript
const invoice = {
  invoiceNumber: 123,
  items: [service.createItem('Услуга', 'шт', 1, 1000)],
  payment: { cardNumber: '...', sbpPhone: '...', sbpBank: '...' }
};

await service.createInvoice(invoice);
```

#### `createItem(name, unit, quantity, price)`
#### `createPercentDiscount(percent, description)`
#### `createFixedDiscount(amount, description)`

Удобные методы для создания позиций и скидок (см. InvoiceGenerator ниже).

#### `listInvoices()`

Получает список всех счетов на Яндекс.Диске.

**Возвращает:** Promise<Array> - Список PDF файлов

#### `isYandexDiskConfigured()`

Проверяет, настроен ли Яндекс.Диск.

**Возвращает:** boolean

---

### InvoiceGenerator

#### `createItem(name, unit, quantity, price)`

Создает объект позиции счета с автоматическим расчетом суммы.

**Параметры:**
- `name` (string) - Наименование товара/услуги
- `unit` (string) - Единица измерения
- `quantity` (number) - Количество
- `price` (number) - Цена за единицу

**Возвращает:** InvoiceItem - Объект позиции счета

**Пример:**
```javascript
const generator = new InvoiceGenerator();
const item = generator.createItem('Аренда АТС', 'мес', 1, 5700.00);
// { name: 'Аренда АТС', unit: 'мес', quantity: 1, price: 5700.00, amount: 5700.00 }
```

#### `createPercentDiscount(percent, description)`

Создает объект процентной скидки.

**Параметры:**
- `percent` (number) - Процент скидки (например, 10 для 10%)
- `description` (string, опционально) - Описание скидки

**Возвращает:** Discount - Объект скидки

**Пример:**
```javascript
const discount = generator.createPercentDiscount(10, 'Скидка постоянного клиента');
// { value: 10, type: 'percent', description: 'Скидка постоянного клиента' }
```

#### `createFixedDiscount(amount, description)`

Создает объект фиксированной скидки.

**Параметры:**
- `amount` (number) - Сумма скидки
- `description` (string, опционально) - Описание скидки

**Возвращает:** Discount - Объект скидки

**Пример:**
```javascript
const discount = generator.createFixedDiscount(5000, 'Специальное предложение');
// { value: 5000, type: 'fixed', description: 'Специальное предложение' }
```

#### `generateInvoice(invoiceData, outputPath)`

Генерирует PDF счет.

**Параметры:**
- `invoiceData` (InvoiceData) - Данные для счета
- `outputPath` (string) - Путь для сохранения PDF файла

**Возвращает:** Promise<string> - Путь к созданному файлу

**Пример:**
```javascript
const generator = new InvoiceGenerator();
const filePath = await generator.generateInvoice(invoiceData, './invoice.pdf');
console.log(`Счет создан: ${filePath}`);
```

#### `formatAmount(amount)`

Форматирует число как денежную сумму.

**Параметры:**
- `amount` (number) - Сумма

**Возвращает:** string - Отформатированная сумма (например, "5 700.00")

#### `generateQRCode(payment)`

Генерирует QR-код для платежной информации.

**Параметры:**
- `payment` (PaymentInfo) - Платежная информация

**Возвращает:** Promise<string> - Base64 строка изображения QR-кода

#### `generateAndUploadToYandexDisk(invoiceData, outputPath, yandexToken, remotePath, options)`

Генерирует счет и автоматически загружает его на Яндекс.Диск.

**Параметры:**
- `invoiceData` (InvoiceData) - Данные счета
- `outputPath` (string) - Локальный путь для сохранения PDF
- `yandexToken` (string) - OAuth токен Яндекс.Диска
- `remotePath` (string) - Путь на Яндекс.Диске (например, '/Счета/invoice.pdf')
- `options` (Object, опционально) - Дополнительные опции:
  - `createFolder` (boolean) - Создавать папку, если не существует (по умолчанию: true)
  - `publish` (boolean) - Сделать файл публичным (по умолчанию: false)
  - `deleteLocal` (boolean) - Удалить локальный файл после загрузки (по умолчанию: false)

**Возвращает:** Promise<Object> - Результат с путями и ссылками:
- `success` (boolean) - Успешность операции
- `localPath` (string|null) - Путь к локальному файлу
- `remotePath` (string) - Путь на Яндекс.Диске
- `publicUrl` (string|null) - Публичная ссылка (если publish: true)
- `uploadResult` (Object) - Детали загрузки

**Пример:**
```javascript
const result = await generator.generateAndUploadToYandexDisk(
  invoiceData,
  './output/invoice.pdf',
  process.env.YANDEX_DISK_TOKEN,
  '/Счета/invoice.pdf',
  { publish: true }
);
console.log(result.publicUrl);
```

### YandexDiskService

#### `constructor(oauthToken)`

Создает экземпляр сервиса Яндекс.Диска.

**Параметры:**
- `oauthToken` (string) - OAuth токен для доступа к Яндекс.Диску

#### `getDiskInfo()`

Получает информацию о диске (свободное место, использованное место и т.д.).

**Возвращает:** Promise<Object> - Информация о диске

#### `createFolder(folderPath)`

Создает папку на Яндекс.Диске.

**Параметры:**
- `folderPath` (string) - Путь к папке (например, '/Счета')

**Возвращает:** Promise<Object> - Информация о созданной папке

#### `uploadFile(localFilePath, remotePath, overwrite)`

Загружает файл на Яндекс.Диск.

**Параметры:**
- `localFilePath` (string) - Путь к локальному файлу
- `remotePath` (string) - Путь на Яндекс.Диске
- `overwrite` (boolean, опционально) - Перезаписывать файл, если существует (по умолчанию: true)

**Возвращает:** Promise<Object> - Результат загрузки

#### `publishFile(remotePath)`

Делает файл публичным и возвращает публичную ссылку.

**Параметры:**
- `remotePath` (string) - Путь к файлу на Яндекс.Диске

**Возвращает:** Promise<string> - Публичная ссылка

#### `listFiles(folderPath, limit)`

Получает список файлов в папке.

**Параметры:**
- `folderPath` (string) - Путь к папке
- `limit` (number, опционально) - Максимальное количество файлов (по умолчанию: 100)

**Возвращает:** Promise<Array> - Список файлов

#### `deleteFile(remotePath, permanently)`

Удаляет файл с Яндекс.Диска.

**Параметры:**
- `remotePath` (string) - Путь к файлу на Яндекс.Диске
- `permanently` (boolean, опционально) - Удалить навсегда, не в корзину (по умолчанию: false)

**Возвращает:** Promise<Object> - Результат удаления

## Часто задаваемые вопросы

### Как получить OAuth токен для Яндекс.Диска?

1. Перейдите на https://yandex.ru/dev/disk/poligon/
2. Нажмите "Получить OAuth-токен"
3. Разрешите приложению доступ к вашему Яндекс.Диску
4. Скопируйте полученный токен
5. Добавьте его в файл `.env`

### Безопасно ли хранить токен в .env файле?

Файл `.env` добавлен в `.gitignore` и не будет загружен в репозиторий. Однако для production-среды рекомендуется использовать более защищенные методы хранения секретов (например, переменные окружения сервера, секреты Kubernetes и т.д.).

### Можно ли использовать сервис без Яндекс.Диска?

Да! Интеграция с Яндекс.Диском полностью опциональна. Вы можете использовать только генерацию PDF без загрузки на Яндекс.Диск.

### Как изменить шрифт или стиль счета?

Отредактируйте файл `src/invoiceGenerator.js` и измените параметры PDF документа, шрифты, размеры и цвета по вашему желанию.

## Лицензия

ISC
