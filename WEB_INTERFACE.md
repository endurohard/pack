# 🌐 Веб-интерфейс

## Обзор

Генератор счетов теперь имеет удобный веб-интерфейс! Никакого программирования - просто откройте браузер и создавайте счета.

## Возможности веб-интерфейса

✅ Создание счетов через форму
✅ Добавление неограниченного количества позиций
✅ Настройка скидок (процентных и фиксированных)
✅ Автоматическая загрузка на Яндекс.Диск
✅ Получение публичных ссылок
✅ Скачивание PDF прямо из браузера
✅ Адаптивный дизайн (работает на телефонах)

## Запуск

### Локально

```bash
npm start
```

Откройте: http://localhost:3000

### Docker

```bash
docker-compose up -d
```

Откройте: http://localhost:3000

## Использование

### 1. Заполните основные данные

- **Номер счета** - уникальный номер вашего счета
- **Позиции** - добавьте товары/услуги
  - Название
  - Единица измерения (шт, час, мес и т.д.)
  - Количество
  - Цена

### 2. Добавьте скидку (опционально)

- **Процентная** - например, 10% от суммы
- **Фиксированная** - например, 5000₽ от суммы
- **Описание** - "Скидка постоянного клиента"

### 3. Укажите платежные данные

- Номер карты для оплаты
- Телефон для СБП
- Название банка

### 4. Выберите опции

- ☑️ Загрузить на Яндекс.Диск
- ☑️ Получить публичную ссылку

### 5. Создайте счет!

Нажмите **"Создать счет"** и получите:
- 📄 PDF файл для скачивания
- ☁️ Ссылка на файл в Яндекс.Диске (если включено)
- 🔗 Публичная ссылка для отправки клиенту (если включено)

## API Endpoints

Если хотите интегрировать с другими системами:

### POST /api/invoice

Создать счет

**Body:**
```json
{
  "invoiceNumber": 123,
  "items": [
    {
      "name": "Услуга",
      "unit": "шт",
      "quantity": 1,
      "price": 1000
    }
  ],
  "discount": {
    "type": "percent",
    "value": 10,
    "description": "Скидка"
  },
  "payment": {
    "cardNumber": "1111-2222-3333-4444",
    "sbpPhone": "9001112233",
    "sbpBank": "Банк"
  },
  "uploadToYandex": true,
  "getPublicLink": true
}
```

**Response:**
```json
{
  "success": true,
  "filename": "invoice_123_1234567890.pdf",
  "localPath": "/app/output/invoice_123_1234567890.pdf",
  "yandexPath": "/Счета/invoice_123_1234567890.pdf",
  "publicUrl": "https://disk.yandex.ru/..."
}
```

### GET /download/:filename

Скачать PDF файл

**Example:**
```
GET /download/invoice_123_1234567890.pdf
```

### GET /api/status

Получить статус Яндекс.Диска

**Response:**
```json
{
  "yandexConfigured": true,
  "diskInfo": {
    "usedGB": "5.23",
    "totalGB": "10.00",
    "freeGB": "4.77"
  }
}
```

### GET /api/invoices

Получить список всех счетов

**Response:**
```json
{
  "local": [
    {
      "name": "invoice_123.pdf",
      "size": 24576,
      "created": "2025-01-01T12:00:00.000Z",
      "local": true
    }
  ],
  "yandex": [
    {
      "name": "invoice_124.pdf",
      "size": 25600,
      "created": "2025-01-01T13:00:00.000Z",
      "local": false
    }
  ]
}
```

## Настройка порта

По умолчанию сервис работает на порту **3000**.

Изменить порт:

```bash
# Через переменную окружения
PORT=8080 npm start

# Или в .env файле
PORT=8080
```

## Настройка Яндекс.Диска

Если вы запустили веб-сервис, но не настроили Яндекс.Диск:

1. Остановите сервер (Ctrl+C)
2. Запустите: `npm run setup`
3. Получите токен по инструкции
4. Запустите сервер снова: `npm start`

Или добавьте токен в `.env`:

```env
YANDEX_DISK_TOKEN=ваш_токен
YANDEX_DISK_FOLDER=/Счета
```

## Скриншоты

### Главная страница
![Главная страница](docs/screenshot-main.png)

### Форма создания счета
![Форма](docs/screenshot-form.png)

### Результат
![Результат](docs/screenshot-result.png)

## Безопасность

⚠️ **Внимание:** Это базовый веб-интерфейс без аутентификации!

Для production использования добавьте:
- Аутентификацию (JWT, OAuth)
- HTTPS (SSL/TLS)
- Rate limiting
- CORS настройки
- Валидацию входных данных
- Логирование

## Разработка

Запуск в режиме разработки с автоперезагрузкой:

```bash
npm run dev
```

## Troubleshooting

### Порт занят

```bash
# Найти процесс
lsof -i :3000

# Или измените порт
PORT=8080 npm start
```

### Яндекс.Диск не работает

1. Проверьте `.env` файл
2. Проверьте токен на https://yandex.ru/dev/disk/poligon/
3. Посмотрите логи сервера

### PDF не создается

1. Проверьте, что папка `output/` существует
2. Проверьте права доступа
3. Посмотрите логи сервера

---

**Готово!** Теперь у вас есть полноценный веб-сервис для создания счетов! 🚀
